version: '3.8'

services:
  portal-boletim:
    build: .
    restart: unless-stopped
    ports:
      - "3010:3000"   # Front-end Next.js na porta 3010 do host
      - "3011:3001"   # API Express na porta 3011 do host
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - NODE_ENV=production
    env_file:
      - .env